import { Register } from '@/components';
import React from 'react';

export default function RegisterPage() {
    return (
        <main className="relative w-full h-full min-h-screen mx-auto max-w-7xl">
            <div className="flex items-center justify-center w-full h-screen max-w-lg mx-auto">
                <Register />
            </div>
        </main>
    )
}
